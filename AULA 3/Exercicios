//Aluno Lucas Alves Cordeiro
//Exercicio 1
 Scanner ler = new Scanner(System.in);

          System.out.println("Digite um numero inteiro : ");

          int numero = ler.nextInt();
          if( numero > 20 ){
              double metade = numero / 2.0;
              System.out.println(" A metade do numero é : " + metade);

//Excercicio 2
Scanner usuario = new Scanner(System.in);
        int idade;

        System.out.println(" Qual é sua idade ? ");
        idade = usuario.nextInt();

        if ( idade < 17 ) {
            System.out.println(" Menor de idade ");
        }else if ( idade > 18) {
            System.out.println(" Maior de idade");

//Exercicio 3
Scanner ler = new Scanner(System.in);

        int n1,n2, diferente;

            System.out.println(" Digite o primeiro numero : ");
            n1 = ler.nextInt();
            System.out.println(" Digite o segundo numero : ");
            n2 = ler.nextInt();
            if(n1 == n2 ) {
        }else{

            diferente = (n1 - n2);
            System.out.println("Diferença : "+ diferente);


//Exercicio 4
Scanner ler = new Scanner(System.in);

        int n1,n2;

            System.out.println(" Insira o primeiro numero : ");
            n1 = ler.nextInt();
            System.out.println(" Insira o segundo numero : ");
            n2 = ler.nextInt();

              System.out.println(" O " + n1 + " é maior que " + n2);
                if(n1 > n2 ) {
               System.out.println(" O " + n2 + " é menor que " + n1);
            }else{
//Exercicio 5
 Scanner e = new Scanner(System.in);

           System.out.println(" Informe o numero ");
           int n1 = e.nextInt();
        if ( n1>50 && n1<100){
            System.out.println("  pertece ao  intervalo ");
        }else{
            System.out.println(" Não pertence intervalo ");
//Exercicio 6
 double altura;
        char sexo;
        double pesoIdeal;

        System.out.println("Digite sua altura:");
        altura = entrada.nextDouble();

        System.out.println("Digite seu sexo (M ou F):");
        sexo = entrada.next().charAt(0);

        if (sexo == 'M' || sexo == 'm') {
            pesoIdeal = (72.7 * altura) - 58;
        } else if (sexo == 'F' || sexo == 'f') {
            pesoIdeal = (62.1 * altura) - 44.7;
        } else {
            System.out.println("Sexo inválido");
            return;
        }

        System.out.println("Seu peso ideal é: " + pesoIdeal);

